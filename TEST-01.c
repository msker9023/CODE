#include<stdio.h>
#define ADD(x,y) ((x) + (y))
int main()
{
    // int age = 0;
    // scanf("%d",&age);
    // if (age<18)
    //     printf("少年");
    // else if (age>18 && age<20)
    //     printf("qingnian");
    // else
    //     printf("zhuangnian");
    // return 0;
    int i = 1;
    while (i<=100)
    {
        if (i % 2 == 1)
            printf("%d\n",i);
        i += 1;
    -
    }
    return 0;
}